/**
 * PostCSS configuration. This file registers Tailwind CSS and Autoprefixer
 * so that modern CSS can be written with confidence that vendor prefixes
 * and utility classes will be generated appropriately.
 */
export default {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
};